<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} traceId=%X{traceId:-} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="CONSOLE"/>
    </appender>

    <logger name="org.springframework.web" level="INFO"/>

    <springProfile name="!prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/rankademy.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/rankademy.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>50MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>5GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} traceId=%X{traceId:-} - %msg%n</pattern>
            </encoder>
        </appender>

        <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>2048</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <appender-ref ref="FILE"/>
        </appender>

        <logger name="org.hibernate.SQL" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
<!--        <appender name="CLOUDWATCH" class="com.kdgregory.logging.logback.CloudWatchAppender">-->
<!--            <logGroup>${LOG_GROUP:-rankademy-api}</logGroup>-->
<!--            <logStream>${LOG_STREAM:-${HOSTNAME}}</logStream>-->
<!--            <region>${AWS_REGION:-ap-northeast-2}</region>-->
<!--            <retentionPeriod>${LOG_RETENTION_DAYS:-30}</retentionPeriod>-->
<!--            <batchDelay>5000</batchDelay>-->
<!--            <maxBatchSize>256</maxBatchSize>-->
<!--            <truncateOversizeMessages>true</truncateOversizeMessages>-->
<!--            <encoder>-->
<!--                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} traceId=%X{traceId:-} - %msg%n</pattern>-->
<!--            </encoder>-->
<!--        </appender>-->

<!--        <appender name="ASYNC_CLOUDWATCH" class="ch.qos.logback.classic.AsyncAppender">-->
<!--            <queueSize>4096</queueSize>-->
<!--            <discardingThreshold>0</discardingThreshold>-->
<!--            <appender-ref ref="CLOUDWATCH"/>-->
<!--        </appender>-->

        <logger name="org.hibernate.SQL" level="OFF"/>

        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE"/>
<!--            <appender-ref ref="ASYNC_CLOUDWATCH"/>-->
        </root>
    </springProfile>
</configuration>
